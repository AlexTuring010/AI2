# HW3 is inference-only and runs on a GPU host (Kaggle / Colab), not on this laptop.
# On Kaggle, install the latest transformers from source right after a kernel restart
# (Qwen3.5 needs it):
#   !pip install -q -U "transformers @ git+https://github.com/huggingface/transformers.git"
#
# This file documents the stack for reference / local tooling.
transformers @ git+https://github.com/huggingface/transformers.git
torch>=2.0
datasets>=2.14
accelerate>=0.30
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
matplotlib>=3.7
sentencepiece>=0.2
protobuf>=4.25
